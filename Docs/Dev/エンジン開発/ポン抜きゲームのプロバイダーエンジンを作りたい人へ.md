# ポン抜きゲームのプロバイダーエンジンを作りたい人へ

## 役割

App Provider Engineは、ポン抜きゲームの初期局面作成、着手の監視、アゲハマの集計、終局判定と勝者決定を担当します。GUI自身は「アゲハマ20個で勝ち」を判定せず、プロバイダーの応答を信頼して結果画面へ進みます。

GTPと同じく、GUIがコマンドを送り、エンジンが成功応答 `=` または失敗応答 `?` を返します。独自コマンドの正式名は `kfw-` 接頭辞のkebab-caseです。応答本文の構造化データには1行のJSONを使います。

## 能力確認

GUIの［RECHECK PROVIDER］は次を送ります。

```text
known_command kfw-start-app
known_command kfw-end-app
known_command kfw-listen-move
```

両方に対応するエンジンは、それぞれ `= true` を返してください。

## ゲーム設定と開始

Providerは`kfw-describe-options ponnuki provider`で、少なくとも`BoardSize`、`InitialMoveCount`、`RandomSeed`を公開します。GUIはこのスキーマから［GAME SETTINGS］を組み立て、開始前に`kfw-patch-options ponnuki provider {...}`で値を適用します。

盤サイズはID名ではなく`binding`で標準GTPへ結び付けます。離散的な対応サイズには`enum`を推奨します。

```json
{"id":"BoardSize","label":"BoardSize","type":"enum","default":"9","values":["9","13","19"],"binding":"gtp.boardsize","apply":"next-start"}
```

GUIはProviderの候補と自身の対応サイズを照合し、GUI未対応の候補も理由付きのグレー表示で残します。選択値は標準GTP `boardsize`の成功応答で初めて実際の盤サイズとして確定します。

設定画面で値が変わるたび、GUIは未確定値をProviderへ問い合わせます。このコマンドはProviderの現在値を変更しません。

```text
kfw-evaluate-options ponnuki provider {"version":1,"values":{"BoardSize":13,"InitialMoveCount":90}}
```

成功応答は、評価後の全`values`、評価後の全`options`スキーマ、Providerが調整した内容を表す`adjustments`を返します。GUIは応答を画面用キャッシュとして採用します。例えば`InitialMoveCount`の上限は`BoardSize * BoardSize / 4`で、9路は20、13路は42、19路は90です。上限を超えた候補値はProviderが上限へ調整します。

［OK］で保存された値だけを、開始時に`kfw-patch-options`で正式適用します。［CANCEL］された未確定値はProviderへ適用しません。

```text
kfw-start-app ponnuki provider
```

Providerは適用済みオプションを使って初期局面を作り、成功応答へ局面JSONを返します。`RandomSeed`が0なら開始時に自動生成し、1以上なら同じ初期局面を再現します。実際に使ったシードは応答の`seed`へ必ず格納してください。

終了時は状態を解放します。

```text
kfw-end-app ponnuki provider
```

```text
= {"app":"ponnuki","version":1,"boardSize":9,"black":["D4"],"white":["E4"],"toPlay":"black","captures":{"black":0,"white":0},"seed":12345}
```

`black`と`white`は初期配置として扱います。それ以前の仮想手順で生じたアゲハマは持ち越さず、ゲーム開始時は双方0個です。プロバイダーはこの局面と手番を保持し、同じプロセスで後続の着手通知を待ってください。

GUIは応答されたシードをGUI操作ログにも記録します。これを指定付きコマンドへ戻すことで、検討時に同じ初期局面を再現できます。

## 着手通知と終局判定

GUIは、盤上へ反映された着手ごとに次を送ります。

```text
kfw-listen-move A3
```

パスは次の形です。

```text
kfw-listen-move pass
```

プロバイダーは保持している盤面へ現在手番の着手を適用し、取った石を現在手番側のアゲハマへ加算します。継続時の例です。

```text
= {"accepted":true,"gameOver":false,"winner":"","reason":"","blackCaptures":7,"whiteCaptures":4,"nextToPlay":"white"}
```

どちらかがアゲハマ20個へ達した直後の例です。

```text
= {"accepted":true,"gameOver":true,"winner":"black","reason":"PONNUKI BLACK CAPTURED 20","blackCaptures":20,"whiteCaptures":12,"nextToPlay":"white"}
```

`gameOver: true` を受けたGUIは対局中画面から結果画面へ遷移します。`winner`は `black`、`white`、または引き分けを表す空文字列です。

通知された手が局面と一致しない場合は、成功扱いにせずGTP失敗応答を返してください。

```text
? illegal provider move notification
```

## 最小対応コマンド

- 標準GTP: `protocol_version`、`name`、`version`、`known_command`、`list_commands`、`quit`
- 独自拡張: `kfw-describe-options`、`kfw-evaluate-options`、`kfw-patch-options`、`kfw-start-app`、`kfw-end-app`、`kfw-listen-move`

現在の勝利条件は「先にアゲハマ20個」です。将来ルールを増やす場合は、既存バージョンの意味を変更せず、新しいAppプロトコルバージョンとして追加してください。

## 互換性

旧GUIとの互換用に、従来の`kfw-make-position ponnuki 1 9 20 [seed]`も残せます。新GUIはstart/endの両方が利用できれば新方式を優先し、未対応Providerだけ旧方式へフォールバックします。

`binding`を持たない旧スキーマの`BoardSize`は、同名でも標準GTP盤サイズとして扱われません。Provider固有の通常オプションとして表示されます。
