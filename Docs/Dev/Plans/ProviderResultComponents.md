# Provider宣言による結果画面拡張

現行画面は[GUI組み込み構成](../CurrentState/AppLifecycle.md)です。

- Providerが将来宣言する結果コンポーネントを、GUIが用意した共通部品と空き領域へ配置する。
- 将来の結果画面拡張では、表示したい意味的なコンポーネント種別と値を宣言する。ピクセル座標や画面サイズは指定しない。

Providerは例えば `captures` を要求し、GUIが皿・捕獲石・個数の描画と配置を担当する案です。
