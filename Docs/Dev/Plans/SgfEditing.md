# SGF変化図のGUI編集計画

[文書モデルとGo変換は実装済み](../CurrentState/SgfDocument.md)です（2026-08-29の記録）。次は変化図のGUI編集を扱います。

## 次の作業

1. 現行GUIが `SgfDocument` を保持する範囲と、主手順だけの `GoGameRecord` へ縮約する入口を調べる。
2. 変化図とコメントをGUIで保持・編集する経路を接続する。
3. 編集・保存後も複数ゲーム木、複数値、未知プロパティが失われないことを確認する。
4. 既存の読込、保存、レビュー、CGOS解析情報の回帰を確認する。

## 完了条件

変化図をGUIで編集して再読込でき、文書の保持と主手順への投影を混同しない。MatchへSGF固有型やファイル入出力を持ち込まない。

[当初の全実装段階・完了条件](../Archive/SgfDocumentPlan.md)
