# 続きはここから

最終更新: 2026-07-30

## 次の優先作業

Core／Windowsの物理的なプロジェクト分割まで完了した。

- 今後のトップ `README.md` は、配布ZIPを展開してexeをダブルクリックする利用者向けに書く。
- ビルド、テスト、設計、移植、リリースは、トップの `README.developer.md` と共有開発文書へ分離する。
- 開発日誌とリリースノートは「ユーザー向けの説明」を先に置き、「開発者向けの説明」を別節にする。詳細は `Docs/運用/文書の読者分離ルール.md`。
- v2.8.0のReleaseビルド、移植性／Windowsスモーク、GUI／Engine publish、ZIP構成検査まで完了した。
- v2.8.0では、CGOS自動観戦の画面別制御、接続画面のPlayer優先配置、結果画面の導線統一、横長背景、Windows標準に近いテキスト編集とUndo／Redoを追加した。
- GUI版ZIP SHA-256: `26B5D3D42EED18DDC5E77300D7486213FA187424A04081115DD6F914C2458104`。
- Engine版ZIP SHA-256: `8AF75A841FCABCAE555236A1A017FE59E07A2E555070EDF8FB0BD75C1E91D2B4`。
- v2.7.0のReleaseビルド、両スモーク、GUI/Engine publish、ZIP構成検査、SHA-256記録まで完了した。
- v2.7.0の中心機能は、大会ルール、GTPエンジン、CGOS接続先で共用する順序編集。
- GUI版ZIP SHA-256: `2981DB53C2E70F2AA16B283BC388B2A76F8F10195845404894119AA51854EDB4`。
- Engine版ZIP SHA-256: `BDAB11032E958E2FF461AFDBF638B9197DCA1AB2435D071C2A5AF384032C2DC3`。
- 大会ルール設定、GTPエンジン設定、CGOS接続先へ、共通の順序編集ポップアップを追加した。
- CGOS接続先もドラッグ、先頭移動、1件移動、1ページ移動、キャンセル、保存に対応し、並べ替え後の選択対象を維持する。
- リリース初期値を `KifuwarabeGo2026.Gui/default-settings.json` へ集約した。大会ルール、GTPエンジン、CGOS接続先をJSONから調整できる。
- `default-settings.json` のスキーマはバージョン1。大会時間は `TimeControl.Main: "時:分:秒"`、ルール名はPascalCase、内部IDは自動生成。旧時間フィールドと小文字enumは互換読込する。
- 利用者設定は従来どおり `%LOCALAPPDATA%\KifuwarabeGo2026` に保存し、同梱JSONは新規環境の初期生成時だけ使う。
- 旧 `Content/CgosConnections/cgos-connection-list.json` は未使用だったため削除した。
- 一覧の `ORDER` から2列カードボードを開き、隣接2ページ間のドラッグ、先頭移動、1件移動、1ページ移動ができる。
- `CANCEL` は元一覧を維持し、`SAVE` は設定へ順序を保存する。エンジン順変更後も黒白とCGOS黒白の選択対象は維持する。
- 共通状態 `Application/CatalogOrderEditor.cs`、共通描画 `Presentation/Shared/GoScreenRenderer.CatalogOrder.cs`、共通配置 `Presentation/Shared/CatalogOrderEditorLayout.cs`。
- 順序編集の回帰スモークを追加。全体Debugビルドは警告0・エラー0、移植性スモークは `PASS`。実画面でのドラッグ操作確認は未実施。
- 別件として、着手チャートのスコアY軸を内容に応じて自動拡張するよう修正済み。最大絶対値20以下は±20を維持し、20超は5%の余白を付けて10刻みに切り上げる。
- `MoveTrendScoreAxis` に範囲計算を分離し、左Y軸ラベルとスコア棒の座標変換で共有する。
- 最大絶対値21→±30、47→±50、50→±60などの境界値スモークを追加。7プロジェクト全体のDebugビルドは警告0・エラー0、移植性スモークは`PASS`。
- チャート下部の黄色いコメント印は、連続時に線に見えるため削除した。
- コメント表示へ隣接した `|< COMMENT`／`COMMENT >|` 頭出しボタンを追加。前後のコメント付き着手へ移動し、端では無効、ループなし。
- 見出しを `COMMENT n / total   MOVE m`、本文ページを別段の `PAGE p / total` とし、コメント通し番号とページ番号を区別した。
- 棋譜レビューでは盤面もコメント着手へ同期し、ローカル／CGOSチャートでは既存のリプレイseekへ同期する。
- コメント件数、通し番号、前後検索、端でループしないことを移植性スモークへ追加。7プロジェクト全体のDebugビルドは警告0・エラー0、移植性／Windowsスモークは両方`PASS`。
- 詳細: [開発/クロスプラットフォーム対応のためのWindows依存分離計画.md](./開発/クロスプラットフォーム対応のためのWindows依存分離計画.md)
- 共通GUIは `KifuwarabeGo2026.Gui/KifuwarabeGo2026.Gui.Core.csproj`。`net8.0` 単独ビルドが警告0・エラー0で成功済み。
- Windows起動側は `KifuwarabeGo2026.Gui.Windows/KifuwarabeGo2026.Gui.Windows.csproj`。
- `Program.cs` とWindows固有実装は `KifuwarabeGo2026.Gui.Windows` へ移動済み。
- Windows起動側の `AssemblyName` は `KifuwarabeGo2026.Gui`。生成される実行ファイル名も `KifuwarabeGo2026.Gui.exe`。
- MonoGame Content、アイコン、manifest、CGOS同梱publishはWindows起動プロジェクトから参照・実行する。
- TEMPへの `win-x64` publishが成功し、GUI exe、Core DLL、Content、`Tools/Cgos` を確認済み。
- 通常出力先で5プロジェクトのsolution buildが警告0・エラー0で成功済み。
- 新WindowsプロジェクトのDebug版を起動し、応答中のタイトル画面、Content読込、クリックログ、例外0件を確認済み。
- 第9段階で起動したテスト用GUIは、通常の終了要求で終了済み。
- `KifuwarabeGo2026.Gui.PortabilitySmoke` を追加し、Windows APIなしで8個のプラットフォーム契約と `Game1` の構成例がコンパイルできるようにした。
- 6プロジェクトのsolution buildが警告0・エラー0で成功し、ウィンドウを開かない移植性スモーク実行も成功済み。
- [設計/きふわらべの碁2026・移植の手引き.md](./設計/きふわらべの碁2026・移植の手引き.md) を追加。作者の確認環境はWindowsのみであり、Linux/macOSへの移植協力を歓迎する方針と、移植手順・確認項目を記載した。
- 移植性スモークを自動回帰検査へ拡張。Coreの `net8.0`、Windows専用アセンブリ参照なし、P/Invokeなし、代替サービスの安全動作、`Game1` 構成例を実行時に確認する。
- 第12段階後の6プロジェクト全体ビルドは警告0・エラー0。移植性スモークは `PASS` で終了コード0。
- `.github/workflows/portability.yml` を追加し、Ubuntu・macOS・WindowsでCoreのReleaseビルドと移植性スモークを行うCIを用意した。
- CIはまだGitHubへpushされていないため、GitHub-hosted runner上の初回結果は未確認。ローカルWindowsでは同じrestore・Releaseビルド・移植性スモークが警告0・エラー0、`PASS`で成功済み。
- `.slnx` は.NET SDK 9.0.200以降のCLI対応であるため、開発用SDKを10.0.302へ統一した。`global.json` は `rollForward: latestFeature`、`allowPrerelease: false`。対象フレームワークと利用者向けランタイムは.NET 8のまま。
- SDK統一後、solution全体のDebugビルドは警告0・エラー0。Release移植性スモークも`PASS`。
- Windows CIへsolution全体のReleaseビルド、GUI `win-x64` publish、必須配布ファイル検査、同梱CGOSの `--help` 起動を追加した。
- CIと同じWindows配布物検査をローカル実行し、RID付きrestore、publish、9個の必須ファイル、同梱CGOS起動がすべて成功した。
- `KifuwarabeGo2026.Gui.WindowsSmoke` を追加。Windowsサービス生成、実行ファイル命名、文字PNG、バージョン、埋込みアイコン資源をGUIなしで検査し、`PASS`。
- 7プロジェクト全体のReleaseビルドが警告0・エラー0で成功。
- 移植基盤の実装と自動検査は約90%完了。残りはWindows GUIの手動回帰確認と、push後のGitHub-hosted runner初回結果。Linux/macOSの製品GUI実装は実機を持つ協力者向け作業。
- [開発/Windows GUI手動スモークテスト手順.md](./開発/Windows GUI手動スモークテスト手順.md) を追加。手動確認をAからEの短いラウンドへ分割した。
- ラウンドAは利用者確認で全項目OK。基本表示、アイコン、`CURRENT`、人間同士のローカル対局、着手、投了、画面復帰、正常終了を確認済み。
- ラウンドBも利用者確認で全項目OK。設定画面、2種類のフォルダー選択キャンセル、2種類のExplorer起動、GUIログ外部起動、画面復帰、正常終了を確認済み。
- 2026-07-29はここで安全に中断。テスト用GUIは終了済み。
- v2.6.0のReleaseビルド、移植性スモーク、Windowsスモーク、GUI/Engineのpublish、配布ZIP検査は完了。
- v2.6.0のGUI版・Engine版ZIPを作成し、SHA-256を開発日誌とリリースノートへ記録した。
- v2.6.0の公開後、残るWindows GUI手動スモークテストはラウンドCのSGF保存・読込から再開する。
- 詳細と残る手動項目: [開発/Core・Windows分割後スモークテスト.md](./開発/Core・Windows分割後スモークテスト.md)
- 次はアイコン、SGF、GTP設定、クリップボード、外部ログ、CGOSの画面操作を手動確認する。
- 現在の正式対応 OS と配布物はWindowsのまま。

## 現在の状態

- ソリューションは `KifuwarabeGo2026.Gui.Core`、`KifuwarabeGo2026.Gui.Windows`、`KifuwarabeGo2026.Gui.PortabilitySmoke`、`KifuwarabeGo2026.Gui.WindowsSmoke`、`KifuwarabeGo2026.Engine`、`KifuwarabeGo2026.Shared`、`KifuwarabeGo2026.Gui.Communication.Cgos` の7プロジェクト構成。
- `GoBoard`、`GoPoint`、`GoStone`、`GoRenParseResult` はSharedへ移動済み。
- GUIとEngineはSharedを参照し、EngineからGUIへの依存はない。
- CGOS通信クライアントはGUIが外部プロセスとして起動する。
- GTPエンジン設定は表示名とは別に、CGOSのデフォルトログイン名と平文パスワードを保存できる。
- CGOS接続画面では、黒白プレイヤーごとにログイン名とパスワードを接続前に編集できる。
- 利用者向け配布物はGUI版とEngine版の2種類だけ。
- GUIをpublishすると、CGOS通信コンポーネントが `Tools\Cgos` へ自動的にpublishされる。
- SharedはGUI版とEngine版へ自動的に含まれるため、単独publish・単独配布は不要。

## リリース手順

最初に [開発/リリース手順.md](./開発/リリース手順.md) を読む。

リリース用コマンドは次の2本。

```powershell
dotnet publish KifuwarabeGo2026.Gui.Windows\KifuwarabeGo2026.Gui.Windows.csproj -c Release -r win-x64 --self-contained false
dotnet publish KifuwarabeGo2026.Engine\KifuwarabeGo2026.Engine.csproj -c Release -r win-x64 --self-contained false
```

CGOSとSharedを個別にpublishしないこと。

## 確認済み

- v2.0.0をGitHub Releasesへ公開: https://github.com/muzudho/KifuwarabeGo2026/releases/tag/v2.0.0
- v1.3.0をGitHub Releasesへ公開済み: https://github.com/muzudho/KifuwarabeGo2026/releases/tag/v1.3.0
- v1.2.0をGitHub Releasesへ公開済み: https://github.com/muzudho/KifuwarabeGo2026/releases/tag/v1.2.0
- 2026-07-20にv1.3.0のGUI版・Engine版ZIPを `Uploads` に作成済み。
- GUI版ZIPはGUI、Shared、`Tools\Cgos` を含み、Engine版ZIPはEngineとSharedを含むことを確認。
- GUI、Engine、CGOSのexeはファイルバージョン `1.3.0.0` を確認。
- 2026-07-20にソリューション全体のReleaseビルドが警告0・エラー0で成功。
- 2026-07-23にCGOS資格情報欄の追加後、ソリューション全体のDebugビルドが警告0・エラー0で成功。
- 2026-07-29に移植性スモーク追加後、6プロジェクトのDebugビルドと移植性スモーク実行が成功。
- GUI版とEngine版のRelease `win-x64` publishが成功。
- GUI版の `Tools\Cgos` にCGOSのexe、dll、deps.json、runtimeconfig.jsonが入ることを確認。
- GUI版とEngine版の両方に `KifuwarabeGo2026.Shared.dll` が入ることを確認。
- GUI版へ同梱されたCGOS実行ファイルの `--help` 起動に成功。
- EngineのGTP基本応答、バージョン `1.3.0`、`cgos-genmove_analyze` 対応を確認。
- GUI版ZIPのSHA-256は `B0E7A947EB20577D5384DF257056A93CB18C6EABC4B0F9CCF7526415BDFD9E4C`。
- Engine版ZIPのSHA-256は `20C47502FC63BBE3A04C867D5432EA6EA4149483C3C80694393C1E4855E56474`。
- 2026-07-23にv2.0.0のReleaseビルドとGUI版・Engine版のwin-x64 publishが警告0・エラー0で成功。
- GUI、CGOS通信コンポーネント、Engineのファイルバージョンはすべて `2.0.0.0`。
- EngineのGTP基本応答と `version 2.0.0`、同梱CGOS通信コンポーネントの `--help` 起動を確認。
- v2.0.0 GUI版ZIPにGUI、Shared、`Tools/Cgos` の必須ファイルが入り、旧 `KifuwarabeGo2026.exe` が含まれないことを確認。
- v2.0.0 GUI版ZIPのSHA-256は `54DB4431C531D90CE60D67118ACB1A36F4DEA6BD539B6BA7CBA06F7A356D0D4E`。
- v2.0.0 Engine版ZIPのSHA-256は `E82E37313DC92CEE69C7E4E566F8B18A0550D3E92C645FDC66E9874460B750C9`。

## 次に行うこと

- 盤面編集後に黒番・白番を両方 `Computer` にしてローカル対局すると、エンジンエラーになる現象を調査する。
- CGFオープン会場で、黒白それぞれの保存済みデフォルト資格情報によるCGOS実接続を確認する。
- `logs/errors/application-error-*.log` と `logs/gtp.log` を確認し、編集局面の GTP エンジン同期または初期化処理を追う。
- 画面スタイルは結果画面を基準に、休憩、ローカル対局、棋譜レビュー、盤面編集まで統一済み。
- 盤座標は左下原点の国際式で、下辺の列文字をワインレッド、左辺の行番号を青緑系の半透明アクリル風表示にしている。
- 次回リリースでも、GUI版とEngine版の2配布を維持する。
- 詳細な定型作業は [開発/リリース手順.md](./開発/リリース手順.md) に従う。

## 主な実装場所

- GUI publish時のCGOS同梱: `KifuwarabeGo2026.Gui.Windows/KifuwarabeGo2026.Gui.Windows.csproj`
- Linux/macOS移植時の構成例: `KifuwarabeGo2026.Gui.PortabilitySmoke/`
- Windows固有サービスの非対話検査: `KifuwarabeGo2026.Gui.WindowsSmoke/`
- 同梱CGOSの探索: `KifuwarabeGo2026.Gui/Application/Cgos/Connect/CgosConnectionProcess.cs`
- CGOS資格情報の保存: `KifuwarabeGo2026.Gui/Application/GtpEngineProfile.cs`
- CGOS資格情報の画面入力: `KifuwarabeGo2026.Gui/Game1.cs`
- Sharedプロジェクト: `KifuwarabeGo2026.Shared/KifuwarabeGo2026.Shared.csproj`
- ソリューション構成: `KifuwarabeGo2026.slnx`
- 共通盤面と座標表示: `KifuwarabeGo2026.Gui/Presentation/Shared/Board/GoScreenRenderer.Board.cs`
- 座標専用フォント: `KifuwarabeGo2026.Gui/Content/Fonts/BoardCoordinate.spritefont`
- 2026年7月の作業記録: `KifuwarabeGo2026.Gui/Docs/開発/開発日誌/2026-07.md`
