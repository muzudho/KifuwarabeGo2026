# 開発日誌 2026年8月

## 2026-08-02

### Go AppsエンジンオプションJSONプロトコル

- 正規コマンドとして`kfw-describe-options`、`kfw-get-options`、`kfw-patch-options`、`kfw-invoke-option`を追加しました。
- すべてのコマンドでアプリIDと`player`／`provider`の役割を指定します。
- 型をUSI風の`check`、`spin`、`combo`、`button`から、JSON本来の`boolean`、`integer`、`enum`、`action`などへ整理しました。
- `kfw-patch-options`は差分更新であり、全項目の検証に成功した場合だけ一括反映します。一項目でも失敗した場合は何も変更せず、JSONエラーを返します。
- 副作用を持つ`action`はパッチへ含めず、`kfw-invoke-option`で独立して実行します。
- 定義へ`immediate`、`next-game`、`restart`の適用タイミングを追加しました。
- `ponnuki provider`の空スキーマと、未対応のapp-roleに対するGTPエラーを区別しました。
- GUI、ポン抜きProvider、Play／ポン抜きPlayer、CGOS通信を新方式優先へ変更し、旧`kfw-options`、`kfw-get-option`、`kfw-set-option`へのフォールバックを維持しました。
- WindowsSmokeへ、型付き値、原子的ロールバック、action分離、旧互換、GUIの外部プロセス適用を検査するテストを追加しました。
- Debugビルドは警告0・エラー0で、WindowsSmokeとPortabilitySmokeはともに`PASS`しました。

### v3.1.0 リリース

- GUI、Core、Engine、Shared、Match、GtpExtensions、CGOS通信コンポーネントのバージョンを`3.1.0`へ更新しました。
- `RELEASE_NOTES_v3.1.0.md`を作成し、公開リファレンス、新オプションプロトコル、KFAからKFWへの移行、MCP警告のトラブルシューティングをまとめました。
- Releaseビルドは警告0・エラー0で、WindowsSmokeとPortabilitySmokeはともに`PASS`しました。
- GUI版とEngine版をWindows x64向けにpublishし、GUI、Core、Shared、Match、GtpExtensions、CGOS、Engineのファイルバージョン`3.1.0.0`を確認しました。
- 同梱CGOSの`--help`、Engineのversion `3.1.0`、`kfw-describe-options`、`kfw-patch-options`、型付きスキーマ応答を確認しました。
- GUI版ZIP SHA-256: `4907298D86D3C512358C8A47BFA842C19946D295851A369CE53FC3D1EB3B79E4`
- Engine版ZIP SHA-256: `162BF0FF6A552B1CB53844ACC4A921210BF5304DF3484FFF2A8F90220FEC2AB6`
- リリースコミット`5ec0d2f`へ注釈付きタグ`v3.1.0`を作成し、originへpushしました。
- GitHub Release `v3.1.0`をdraftではない通常の最新リリースとして公開し、GUI版とEngine版のZIPを添付しました。
- GitHub側で表示された両ZIPのSHA-256が、ローカルで計算してリリースノートへ記載した値と一致することを確認しました。
- 公開先: https://github.com/muzudho/KifuwarabeGo2026/releases/tag/v3.1.0

## 2026-08-01

### Matchプロジェクトの分離

- ローカル対局の中核を `KifuwarabeGo2026.Match` へ分離しました。
- MatchはWindows、GUI、通信、外部プロセスへ依存せず、将来のUbuntu上の囲碁サーバーからもラッピングできる `net8.0` ライブラリです。
- 盤サイズ、コミ、初期配置、開始手番、着手、パス、投了、手数上限、履歴をMatchで管理します。
- 人間対コンピューターとコンピューター同士を含むローカル対局をMatchへ接続しました。
- サーバー確定時計のスナップショットと観戦イベント、黒白の結果申告、合意、再開、審判裁定を追加しました。
- 認証、WebSocket、永続化、審判権限確認はMatchへ入れず、将来のサーバー側ラッパーが担当する境界を維持しました。

### GtpExtensionsと指定局面コンシェルジュ

- `KifuwarabeGo2026.GtpExtensions` を追加し、Windows、MonoGame、GUI、Engine、外部プロセスに依存しない指定局面互換層を作りました。
- 初期局面を空、標準置き碁、自由置き碁、黒白混在、履歴依存へ分類します。
- 9路、13路、19路の標準置石、GTP座標、`fixed_handicap`、`set_free_handicap`、`loadsgf`、連続 `play` のStrategyを追加しました。
- `name`、`version`、`known_command`、`list_commands` を使い、Supported、Unsupported、Unknownと診断根拠を保持する能力検査を追加しました。
- `loadsgf` 用の最小SGFをUTF-8 BOMなしで一時保存し、AB、AW、PL、SZ、KMとパス引用方式に対応しました。
- 明確なコマンド拒否だけを自動フォールバックし、未検証成功、局面不一致、想定外応答、通信障害では利用者判断を待つConciergeを実装しました。
- 黒番と白番の2エンジンは、能力、試行履歴、診断、次方式、承認状態を独立して保持します。

### 指定局面GUI

- 初期石のあるコンピューター対局で、右側へ［指定局面コンシェルジュ］を表示するようにしました。
- 方式ごとの成功、未確認、非対応、拒否、不一致、想定外応答、通信失敗を画面へ表示します。
- ［別の方法を試す］［このまま続ける］［GTP LOG］［CANCEL］をマウスとキーボードで操作できます。
- 全エンジンが検証済みまたは利用者承認済みになるまで、思考開始を抑止します。
- 日本語を英数字用SpriteFontへ渡した文字化けを、OS文字ラスタライズ経路へ切り替えて修正しました。
- コンシェルジュのクリックが背後の小型チャートへ伝播する問題を修正しました。

### エンジンProfileと成功方式の保存

- Generic、Kifuwarabe、KataGo、Leela Zero、GNU Goの互換Profileを追加しました。
- エンジン編集画面の `POSITION` 欄で、Profileと手動優先方式を選択できます。
- 自動確認または利用者承認された方式を、エンジン名、version、Profileと一緒に保存します。
- 同じ組み合わせでは成功方式を優先し、nameまたはversionが変わった場合は自動結果だけを破棄して再検査します。
- Profileへ `ConservativeFallback`、`OfficialDocumentationOnly`、`BundledEngineVerified` の証拠区分を追加しました。
- 外部エンジンを実機確認していない状態を成功扱いしない方針を、コードと検証文書の両方へ反映しました。

### きふわらべ原子的指定局面プロトコル

- 先に独自GTP拡張仕様を作り、準備盤、commit、abort、失敗時の通常盤保護を定義しました。
- Engineへ追加した原子的指定局面コマンドを、v3.0.0で `kfw-begin-position`、`kfw-add-black`、`kfw-add-white`、`kfw-set-to-play`、`kfw-commit-position`、`kfw-abort-position` へ改名しました。旧snake_case名は互換エイリアスとして残しています。
- 黒白混在、呼吸点のない石、通常の着手順では作れない自由編集形を準備盤へ直接配置できます。
- 成功した `kfw-commit-position` だけが実対局盤を置き換え、エラーまたはabortでは準備盤を破棄します。
- 全6コマンドを `known_command` と `list_commands` へ追加し、きふわらべProfileの最優先Strategyへ接続しました。

### 検査と文書

- 同梱Engine.exeをGUI本番と同じ外部プロセス経路で起動し、能力検査、Profile自動選択、Concierge、原子的commit、`VerifiedSuccess`まで自動検査します。
- 移植性スモークへ分類、Strategy、SGF、能力検査、フォールバック、Profile、JSON保存の回帰検査を追加しました。
- Windowsスモークへ黒白混在、呼吸点のない編集形、重複、盤外、commit失敗、通常盤保護、abortの検査を追加しました。
- 指定局面コンシェルジュ利用ガイド、エンジン検証結果、原子的指定局面GTP拡張仕様を追加しました。
- 利用者による実画面確認で、きふわらべを使った指定局面戦が開始できることを確認しました。

### v2.9.0 リリース

- GUI、Core、Engine、Shared、Match、GtpExtensions、CGOS通信コンポーネントのバージョンを `2.9.0` へ更新しました。
- 指定局面対応計画の第1段階から第10段階までをv2.9.0へ収録します。
- 9プロジェクトのReleaseビルドは警告0・エラー0で成功し、移植性スモークとWindowsスモークはともに `PASS` でした。
- GUIとEngineをWindows x64向けにpublishし、GUI、Core、Shared、Match、GtpExtensions、CGOS、Engineがすべてファイルバージョン `2.9.0.0` であることを確認しました。
- 同梱CGOSの `--help` とEngineのGTP応答を確認し、Engineが `2.9.0` と原子的指定局面6コマンドへの対応を返すことを確認しました。
- 配布物からPDB、旧GUI実行名、利用者用エンジン設定を除外しました。
- GUI版ZIP SHA-256: `78A326BCC9AE262B7CC1BDB669201CD5C11FDEB1636D1ADA8E9298BB79D05925`。
- Engine版ZIP SHA-256: `30A67AB5C39942DDA5AFFF661EC9978E3182148EF0EEED3FEF2CBAF851F365F7`。
- GitHub Release `v2.9.0` を通常リリースとして公開し、GUI版とEngine版のZIPを添付しました。

### Go Play／Go Appsへの再編

- タイトル画面の機能を、通常対局の `Go Play` と、外部エンジンが自由な囲碁アプリケーションを提供できる `Go Apps` に再編しました。
- `Go Play` にはローカル対局とCGOS接続、`Go Apps` にはポン抜きゲームを常時見えるカードとして配置し、アプリの内容を一目で把握できる構成にしました。
- ポン抜きゲームへカーソルを合わせると、ルールと面白さをクリック前に読めるポップアップを表示します。
- タイトルバーを `Kifuwarabe Go 2026 | v3.0.0` とし、製品名とバージョンを明確に分けて表示しました。

### ポン抜きゲーム

- ［問題提供エンジン］を、より広い用途を表せる［APP PROVIDER ENGINE／問題提供エンジン］として選択する画面を追加しました。
- Providerの能力を再確認できる［RECHECK PROVIDER］を追加しました。
- ローカルプレイとローカルアップスで幕間画面を共有し、ポン抜きでは黒プレイヤーと白プレイヤーを選んでから開始する流れにしました。
- Providerが9路盤の初期局面を作成し、GUIがJSON応答から盤面、手番、乱数シードを復元するようにしました。
- GUIは着手ごとに `kfw-listen-move` をProviderへ送り、Providerが着手受理、アゲハマ、終局、勝者、理由をJSONで返します。
- どちらかのアゲハマが20個に達すると、Providerの判定で対局画面から結果画面へ遷移します。
- 省略時の乱数シードは `RandomNumberGenerator.GetInt32` でOSの暗号学的乱数源から偏りなく生成し、応答とGUIログへ記録します。明示シードを渡せば同じ初期局面を再現できます。
- 利用者の実画面確認で、異なる初期局面からポン抜きを遊べることと、アゲハマ20個で結果画面へ進むことを確認しました。

### 独自GTP拡張の整理

- きふわらべ発祥の独自GTP拡張は、正式な接頭辞を短い `kfw-` に統一しました。
- オプション操作、原子的指定局面、Go AppsのProvider操作をkebab-caseの正式名へ揃えました。
- 仕様変更前の `gui_` 系とsnake_case系コマンドは互換エイリアスとして受理し、能力確認とコマンド一覧では正式名を扱います。
- `kfw-make-position ponnuki 1 9 20 [seed]` と `kfw-listen-move <vertex|pass>` を、ポン抜きProviderの基本プロトコルとして実装しました。
- ポン抜きゲームのProviderエンジン作者向け、プレイヤーエンジン作者向けの実装資料を追加しました。

### タイトル画面の囲碁表現

- タイトル背景へ、2D描画を生かしたワイヤーフレームの碁笥と碁石を描く実験的装飾を追加しました。
- 矩形、内接楕円、内接楕円弧、外接円、外接円弧の描画プリミティブを分離し、形、角度、太さを調整しやすくしました。
- 半透明線が正しく合成されるよう、非乗算済みアルファから描画色を生成する方式へ修正しました。
- 日本語を英数字用フォントで描いていた箇所を動的文字描画へ切り替え、連続対局表示とタイトル画面の文字化けを修正しました。

### v3.0.0 リリース

- GUI、Core、Engine、Shared、Match、GtpExtensions、CGOS通信コンポーネントのバージョンを `3.0.0` へ更新しました。
- `Go Problems` から、ゲーム、問題集、教材などを包含できる `Go Apps` への設計変更をメジャーバージョンとして収録します。
- 9プロジェクトのReleaseビルドは警告0・エラー0で成功し、移植性スモークとWindowsスモークはともに `PASS` でした。
- GUIとEngineをWindows x64向けにpublishし、GUI、Core、Shared、CGOS、Engineのファイルバージョンが `3.0.0.0` であることを確認しました。
- 同梱CGOSの `--help`、EngineのGTP基本応答、version `3.0.0`、`kfw-make-position` と `kfw-listen-move` の能力応答を確認しました。
- 配布ZIPからPDBを除外し、GUI版の初期設定 `default-settings.json` は正式な配布ファイルとして残しました。
- GUI版ZIP SHA-256: `FA539A3015DB9A997E102FC4F9F754FB010D5AE318A8093643A5443007B7A828`。
- Engine版ZIP SHA-256: `ABB423FC0E783B4157C6D050182995B055E628272573C6FD324B110600AE089B`。
- GitHub Release `v3.0.0` を通常の最新リリースとして公開し、GUI版とEngine版のZIPを添付しました。
- 公開先: https://github.com/muzudho/KifuwarabeGo2026/releases/tag/v3.0.0
