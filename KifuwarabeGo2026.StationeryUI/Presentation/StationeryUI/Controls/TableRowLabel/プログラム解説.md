# TableRowLabel のプログラム解説

`TableRowLabel` は、表形式の入力画面における各行のラベルです。文字列、位置・サイズ、色、文字倍率を所有します。

描画先は `Draw` に渡すコールバックで抽象化するため、`GoScreenRenderer` へ直接依存しません。`EDIT ENTRY PROFILE` の Player Name、Handle、Engine 行が利用しています。
