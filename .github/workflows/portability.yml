name: Portability

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  core:
    name: Core on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.302

      - name: Restore portability smoke
        run: dotnet restore KifuwarabeGo2026.Gui.PortabilitySmoke/KifuwarabeGo2026.Gui.PortabilitySmoke.csproj

      - name: Build Core and portability smoke
        run: dotnet build KifuwarabeGo2026.Gui.PortabilitySmoke/KifuwarabeGo2026.Gui.PortabilitySmoke.csproj --configuration Release --no-restore

      - name: Run portability checks
        run: dotnet run --project KifuwarabeGo2026.Gui.PortabilitySmoke/KifuwarabeGo2026.Gui.PortabilitySmoke.csproj --configuration Release --no-build

  windows-distribution:
    name: Windows solution and GUI publish
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.302

      - name: Restore solution
        run: dotnet restore KifuwarabeGo2026.slnx

      - name: Build all projects
        run: dotnet build KifuwarabeGo2026.slnx --configuration Release --no-restore

      - name: Run Windows platform checks
        run: dotnet run --project KifuwarabeGo2026.Gui.WindowsSmoke/KifuwarabeGo2026.Gui.WindowsSmoke.csproj --configuration Release --no-build

      - name: Restore Windows GUI for win-x64
        run: dotnet restore KifuwarabeGo2026.Gui.Windows/KifuwarabeGo2026.Gui.Windows.csproj --runtime win-x64

      - name: Publish Windows GUI
        run: dotnet publish KifuwarabeGo2026.Gui.Windows/KifuwarabeGo2026.Gui.Windows.csproj --configuration Release --runtime win-x64 --self-contained false --no-restore

      - name: Verify GUI distribution
        shell: pwsh
        run: |
          $publish = 'KifuwarabeGo2026.Gui.Windows/bin/Release/net8.0-windows/win-x64/publish'
          $required = @(
            'KifuwarabeGo2026.Gui.exe',
            'KifuwarabeGo2026.Gui.Core.dll',
            'KifuwarabeGo2026.Shared.dll',
            'Content/Fonts/Ui.xnb',
            'Content/Fonts/BoardCoordinate.xnb',
            'Tools/Cgos/KifuwarabeGo2026.Gui.Communication.Cgos.exe',
            'Tools/Cgos/KifuwarabeGo2026.Gui.Communication.Cgos.dll',
            'Tools/Cgos/KifuwarabeGo2026.Gui.Communication.Cgos.deps.json',
            'Tools/Cgos/KifuwarabeGo2026.Gui.Communication.Cgos.runtimeconfig.json'
          )
          $missing = $required |
            Where-Object { -not (Test-Path -LiteralPath (Join-Path $publish $_)) }
          if ($missing.Count -gt 0) {
            throw "Missing GUI distribution files: $($missing -join ', ')"
          }
          & (Join-Path $publish 'Tools/Cgos/KifuwarabeGo2026.Gui.Communication.Cgos.exe') --help
