# 続きはここから

最終更新: 2026-07-10

## 現在の前提

- このリポジトリーでは、コンピューター囲碁の思考エンジン `KifuwarabeGo2026` を作る。
- 実装言語は C#。
- 画面表示には MonoGame フレームワークを使う。
- 対局プロトコルには Go Text Protocol (GTP) を使う。
- GTP の入出力は標準入力と標準出力を使う。
- 標準エラー出力は使わない。
- アプリケーション操作には標準入出力を使わず、MonoGame の画面上 UI を使う。

## 決まったこと

- 標準入力と標準出力は GTP 専用にする。
- デバッグログや画面操作結果を標準出力へ混ぜない。
- 画面操作は、マウス、ボタン、テキストボックスなどで行う。
- 以前の Go 言語版 GTP 実装を参考にしつつ、今回は C# で一から書き直す。
- GUI と思考エンジンは別プロセスに分ける方針。
- GUI から思考エンジン `.exe` を起動し、GTP コマンドと応答で連携する。
- 最初の思考エンジンは、強さより別プロセス連携確認を優先し、ランダム合法手エンジンで始める。

## 直近の完了

- GUI のセットアップ画面で、黒番・白番を `Human` / `Computer` から選べるようにした。
- 既定の対局者設定は、黒番 `Human`、白番 `Computer`。
- `KifuwarabeGo2026.Engine` コンソールプロジェクトを追加した。
- エンジンは `protocol_version`、`name`、`version`、`boardsize`、`clear_board`、`play`、`genmove`、`quit` に対応した。
- GUI プロジェクト内に `GtpEngineClient`、`GtpEngineSettings`、`GtpResponse`、`GtpCoordinate` を追加した。
- エンジン単体で `boardsize 9`、`play black D4`、`genmove white` の GTP 応答を確認した。
- GUI の対局開始時に GTP エンジンを起動し、`boardsize` と `clear_board` を送るようにした。
- 人間の着手とパスを `play` コマンドとしてエンジンへ送るようにした。
- `Computer` 手番で `genmove` を送り、返ってきた手を盤へ反映するようにした。
- エンジン準備中や思考中は盤を隠し、`コンピューター準備中` と `対局をキャンセル` ボタンを表示するようにした。
- 準備ができていないのに盤が表示される問題を直した。
- 準備中画面の日本語表示が文字化けしていた問題を直した。
- エンジンエラー時は盤面領域に `エンジンエラー`、エラー内容、GTPログ保存先を表示するようにした。
- 対局中の右パネルに `ENGINE` 状態として `STARTING` / `THINKING` / `READY` / `ERROR` を表示するようにした。

## 現在の注意点

- GUI とエンジンの基本接続は入ったが、長時間対局や異常系の確認はまだ薄い。
- エンジン準備中や思考中は `CanAcceptHumanMove` が false になり、盤面の代わりに準備中画面を出す。
- `GtpEngineClient` はログ保存先として実行ファイル基準の `logs/gtp.log` を使う実装。エラー画面にも保存先を出す。
- エンジン設定はまだ固定の既定値で、設定ファイルや登録 UI は未実装。
- 投了時や終局時にエンジンプロセスをどう閉じるかは未整理。

## 確認済み

- `dotnet build KifuwarabeGo2026.slnx` は成功する。
- 2026-07-10 に `dotnet build KifuwarabeGo2026.slnx` は警告なしで成功。
- エンジン単体の代表コマンド確認:

```powershell
@('protocol_version','name','boardsize 9','clear_board','play black D4','genmove white','quit') | dotnet run --project KifuwarabeGo2026.Engine\KifuwarabeGo2026.Engine.csproj
```

## 参考

- 詳細方針: [設計/基本方針.md](./設計/基本方針.md)
- GTPエンジン連携計画: [開発/GTPエンジン連携実装計画.md](./開発/GTPエンジン連携実装計画.md)
- 最新の開発日誌: [開発/開発日誌/2026-07.md](./開発/開発日誌/2026-07.md)
- 旧 Go 版 GTP 実装: https://github.com/muzudho/kifuwarabe-uec17-golang-from-uec13/blob/main/main_gtp.go

## 次にやる候補

- GUI から対人・対コンピューター戦を何局か触り、`logs/gtp.log` の送受信内容を確認する。
- エンジン起動失敗、GTP エラー、不正な応答手を実際に起こして、表示とキャンセル復帰を確認する。
- `Computer` 同士の対局や、黒番 `Computer` 開始時の挙動を確認する。
- 投了や終局時にエンジンプロセスをどう閉じるか整理する。
- エンジン設定ファイルとエンジン登録 UI の実装へ進む。
